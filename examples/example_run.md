Example Run

The `examples/` folder contains minimal example files (inputs/outputs) for the repository structure.  
There are no commands and no execution instructions here — only data examples.

Files in this folder:
- `sample_inputs.yaml` — example sensor input payload
- `sample_outputs.yaml` — example expected output from one controller step
